class Effect {
    name : string;
    duration : number; // duration in turns

    constructor(name: string, duration: number) {
        this.name = name;
        this.duration = duration;
    }
}
export { Effect };